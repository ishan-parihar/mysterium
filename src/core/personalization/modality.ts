/** 45 uses `GameModality` as the name; the codebase's canonical type is `Modality` (enums.ts). */
export type GameModality = Modality;
export type { Modality } from '../domain/enums.js';
export { ALL_MODALITIES } from '../domain/enums.js';

import type { Modality } from '../domain/enums.js';
