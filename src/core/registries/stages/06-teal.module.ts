import { StageRegistry } from '../index.js';

export function register(): void {
  StageRegistry.register('Teal', {
    stage: 'Teal',
    description: 'Vision-logic, holism. The gateway OPENS (Indigo 6a).',
    stub: true,
  });
}
